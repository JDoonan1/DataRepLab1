//pass 2 values and add them
const additionPartC = (x, y)=> {
    return x + y;
  };

// call function with values for addition and print to console
additionPartC(2,3);
console.log(additionPartC(2,3));